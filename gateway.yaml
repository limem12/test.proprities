eureka:
    client:
        registerWithEureka: true
        fetchRegistry: true
        serviceUrl:
            defaultZone: http://localhost:8080/eureka
spring:
    cloud:
        gateway:
            routes:
                - id : MSProjet
                  uri: lb://MSProjet
                  predicates:
                  - Path=/projects/**
                  filters:
                  - RewritePath=/projects/(?<lien>.*), /${lien}
management:
    endpoints:
        web:
            exposure:
                include: gateway
    endpoint:
        gateway:
            access: read-only
